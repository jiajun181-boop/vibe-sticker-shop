export const metadata = {
  title: "Reset Password | La Lunar Printing Inc.",
  robots: { index: false, follow: false },
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
