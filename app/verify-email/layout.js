export const metadata = {
  title: "Verify Email | La Lunar Printing Inc.",
  robots: { index: false, follow: false },
};

export default function VerifyEmailLayout({ children }) {
  return children;
}
