export const metadata = {
  title: "Forgot Password | La Lunar Printing Inc.",
  robots: { index: false, follow: false },
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}
