export const metadata = {
  title: "Order Cancelled | La Lunar Printing Inc.",
  robots: { index: false, follow: false },
};

export default function CancelLayout({ children }) {
  return children;
}
