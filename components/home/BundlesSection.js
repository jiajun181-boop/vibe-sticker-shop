"use client";

// Bundle feature temporarily disabled — will be re-enabled once core pricing system is live.
// Prices must come from backend, not hardcoded.

export default function BundlesSection() {
  return null;
}
