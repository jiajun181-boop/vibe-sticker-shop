# =============================================================================
# Vibe Sticker Shop - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the real values.
# NEVER commit the .env file to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# PostgreSQL connection string (Neon serverless recommended).
# Use the -pooler endpoint for connection pooling in serverless environments.
DATABASE_URL='postgresql://user:password@ep-example-host-pooler.region.aws.neon.tech/neondb?sslmode=require&channel_binding=require'

# -----------------------------------------------------------------------------
# Stripe (Payments)
# -----------------------------------------------------------------------------
# Publishable key (safe for client-side). Starts with pk_test_ or pk_live_.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key-here

# Secret key (server-side only). Starts with sk_test_ or sk_live_.
STRIPE_SECRET_KEY=sk_test_your-secret-key-here

# Webhook signing secret from the Stripe dashboard (used to verify webhook events).
# Starts with whsec_. Get this from: Stripe Dashboard > Developers > Webhooks.
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret-here

# Currency code for Stripe charges (lowercase ISO 4217).
STRIPE_CURRENCY=cad

# -----------------------------------------------------------------------------
# Admin Authentication
# -----------------------------------------------------------------------------
# Password for admin login (change this to a strong password in production).
ADMIN_PASSWORD=change-me-to-a-strong-password

# Secret used to sign admin JWT tokens. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_JWT_SECRET=generate-a-64-char-hex-string-here

# -----------------------------------------------------------------------------
# Site URL
# -----------------------------------------------------------------------------
# Public-facing URL of the site. Used for SEO, sitemaps, emails, and Stripe redirects.
# Use http://localhost:3000 for local development.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# UploadThing (File Uploads)
# -----------------------------------------------------------------------------
# Token from the UploadThing dashboard.
UPLOADTHING_TOKEN=your-uploadthing-token-here

# -----------------------------------------------------------------------------
# Email (Resend)
# -----------------------------------------------------------------------------
# API key from Resend (https://resend.com). Used for transactional emails
# (order confirmations, password resets, shipping notifications, etc.).
# RESEND_API_KEY=re_your-resend-api-key-here

# From address for outgoing emails. Must be a verified domain in Resend.
# RESEND_FROM_EMAIL=Your Store Name <orders@yourdomain.com>

# Email address that receives contact form submissions.
# CONTACT_NOTIFY_EMAIL=support@yourdomain.com

# -----------------------------------------------------------------------------
# Analytics (Optional)
# -----------------------------------------------------------------------------
# Google Analytics 4 Measurement ID (starts with G-).
# NEXT_PUBLIC_GA4_ID=G-XXXXXXXXXX

# Meta (Facebook) Pixel ID for conversion tracking.
# NEXT_PUBLIC_META_PIXEL_ID=your-pixel-id-here

# -----------------------------------------------------------------------------
# OpenAI (Scripts only - not required for running the app)
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# -----------------------------------------------------------------------------
# Node
# -----------------------------------------------------------------------------
NODE_VERSION=18
